#include <math.h>
#include <stdio.h>	
	int pot(int base, int exp){
	int  res;
    res = pow(base, exp);
	}
    int main (){
   	int base, exp, res;
   	printf("Entre com a base e a potencia ");
   	scanf("%d%d",&base, &exp);
    printf( "O resultado é: %d", res);
}


