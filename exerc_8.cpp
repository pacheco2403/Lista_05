#include <stdio.h>

float p( int sexo , float altura){
	float man, woman;
	int n;
	
printf("1 - M  /  2 - W");
scanf("%d", &n);
}
	int main {
	if(sexo== 1){ 
		h= (72.7 * altura) - 58;
		printf("HOMEM: %.2f", h);
}
	else if (sexo == 2){ 
		m= (62.1 * altura) - 44.7;
		printf("MULHER: %.2f", m);

}
	return man;
	return woman;
}
